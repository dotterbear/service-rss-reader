eureka:
  instance:
    hostname: eureka
    preferIpAddress: true
  client:
    service-url:
      defaultZone: http://${eureka.instance.hostname}:8761/eureka/
spring:
  cloud:
    config:
      label: dev